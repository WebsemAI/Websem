{
  "name": "@websem/types",
  "version": "0.1.3",
  "description": "Shared TypeScript types for websem browser semantic search artifacts",
  "repository": {
    "type": "git",
    "url": "https://github.com/WebsemAI/Websem.git",
    "directory": "packages/typescript/types"
  },
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "format": "prettier --check src",
    "lint": "eslint src",
    "test": "vitest run --passWithNoTests"
  },
  "license": "MIT",
  "publishConfig": {
    "access": "public"
  },
  "engines": {
    "node": ">=20"
  }
}
